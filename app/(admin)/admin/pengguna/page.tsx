export const metadata = { title: "Pengguna" };

export default function AdminPenggunaPage() {
  return (
    <p className="text-muted-foreground">
      Pengelolaan pengguna admin — Phase 10 (User and Role Management).
    </p>
  );
}
