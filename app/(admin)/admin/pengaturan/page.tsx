export const metadata = { title: "Pengaturan" };

export default function AdminPengaturanPage() {
  return (
    <p className="text-muted-foreground">
      Pengaturan aplikasi — setelah auth dan RBAC (Phase 3–4).
    </p>
  );
}
